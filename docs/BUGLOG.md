# BUGLOG — resolved bugs (what · root cause · fix · date)

- Snipe-kill XP granted nothing · AddXP sat inside the token ForEach LoopBody, sniped enemies reserve no token · moved XP to the ForEach Completed pin (Completed→Cast→AddXP→SetDead→SpawnLootDrop), fires once unconditionally; readback-verified (snipe PIE condition not reproducible by user, so code-verified) · 2026-07-17
- GA_Death left player input live ~2s post-death · Disable Input node had zero exec connections (orphaned) · spliced Cast To PlayerController→DisableInput→Delay + wired its PlayerController pin; PIE-confirmed input dead immediately · 2026-07-18
- Enemy damage-after-death, WEAPON path only · weapon hit-scan is a looping timer cleared solely by the HitScanEnd anim-notify, which a death-interrupted montage skips · death chain now calls WeaponActor→HitScanEnd guarded by IsValid(WeaponActor); ability + AOE-field paths remain OPEN (CLAUDE.md §6 item 1) · 2026-07-18
- Extraction point: no [E] prompt, E dead even on a virgin fully-visible instance · the Cube's mesh SM_BigPortal_Space is a Paragon FX mesh with ZERO BodySetup.AggGeom primitives and CTF_USE_DEFAULT, so the player's radius-30 Visibility sphere trace passed through despite component collision reading BlockAllDynamic/QueryAndPhysics/Block-Visibility · user added a Box Collision component (same pattern BP_DroppedItemPickup uses); prior reveal/hide/SetActorEnableCollision/profile-name work was all no-op for this · 2026-07-20
- Boss loot showered twice in one frame (intermittent, ~1 in 3 kills) · boss's Bind Event to On Death hung off Wait for Attribute Changed(Health, OnlyTriggerOnce=false)→Update Health Percentage→Bind, re-binding on every health change; AddUnique usually capped it at 1, hence intermittent · bind moved to BeginPlay (after Parent: BeginPlay so the base still binds first); OnlyTriggerOnce left false deliberately — flipping it freezes the healthbar · 2026-07-20
- Boss death re-entrancy window · Set Dead=true sat at the far tail of the base chain (x=4320) while Call On Death broadcast early (x=544), so the Branch(Dead) guard did not protect the broadcast · Set Dead moved to sit between the guard and Call On Death · 2026-07-20
- Boss loot + extraction reveal silently lost when the healthbar widget ref was invalid · the whole death tail hung off IsValid(BossHealthbarWidgetRef)'s Is Valid pin; Is Not Valid dead-ended · Sequence inserted after RemoveBossHealthBar: then_0 → IsValid → Remove from Parent, then_1 → SpawnBossLoot → reveal · 2026-07-20
- Extraction reveal SetCollisionProfileName was a silent no-op · profile name pin literal read "BlockAllDynamic " with a trailing space; UE does not trim FName pin literals · pin set to exact "BlockAllDynamic" (harmless at the time only because SetCollisionEnabled ran first) · 2026-07-20
